import { StyleSheet } from 'react-native'

const createJob = StyleSheet.create({
    container: {
        flex: 1,
        //alignItems: 'center'
    },

    form: {
        width: '94%',
        marginLeft: 'auto',
        marginRight: 'auto'
    },

    header: {
        marginTop: '5%',
        fontSize: 18,
        alignSelf: 'center',
        //color: '#033e27',
    }
})

export default createJob;